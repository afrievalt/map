module.exports = {
  description: 'create a {{name}}',
  prompts: [
    {
      type: 'input',
      name: 'name',
      message: '{{name}} name?'
    }
  ],
  actions: data => {
    
    const actions = [
      {
        type: 'addMany',
        destination: './',
        base: 'plop-templates/{{namePromptCamel}}-templates/',
        templateFiles: 'plop-templates/{{namePromptCamel}}-templates/*.hbs'
      }
    ]
    return actions
  }
}
