module.exports = {
  description: 'create a {{name}}',
  prompts: [
    {
      type: 'input',
      name: 'name',
      message: '{{name}} name?'
    }
  ],
  actions: data => {
    
    const actions = [
      {
        type: 'addMany',
        destination: './',
        base: 'plop-templates/{{promptNameCamel}}-templates/',
        templateFiles: 'plop-templates/{{promptNameCamel}}-templates/*.hbs'
      }
    ]
    return actions
  }
}
