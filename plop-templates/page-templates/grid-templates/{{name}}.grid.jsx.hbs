import { Container } from '@material-ui/core'
import { DataGrid } from '@material-ui/data-grid'
import {select{{ name }}TableData} from {{lowercase name}}Grid.selectors.js
import React, {memo} from 'react'
import { select{{name}}GridData } from './{{name}}Grid.selectors'

const columns = [

 // { field: 'firstName', headerName: 'First Name', width: 150 },
  
]

const {{name}}Grid = () => {
  return (
    <Container>
        const {{lowercase name}}GridData = useSelector(select{{name}}GridData)
        return (
          <Container style={ { height: 600, width: '100%' } }>
            <DataGrid rows={ {{lowercase name}}GridData} columns={columns} />
          </Container>
        )
    </Container>
  )
}

export default memo({{name}}Grid)
