import { Button, Container, CssBaseline } from '@material-ui/core'
import React, {memo} from 'react'
import InputField from '../../../form/Input.field'

const {{name}}Form = () => {
  return (
    <>
      {{#each field}}
        <InputField fieldId='{{id}}' label='{{id titleCase}}' />
      {{/each}}
    </>
  )
}

export default memo({{name}}Form)
