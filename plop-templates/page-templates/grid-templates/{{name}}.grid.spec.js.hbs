import renderer from 'react-test-renderer'
import AppProvider from '../../AppProvider.js'
import {{name}}Grid from './{{name}}.grid.jsx';

xit('renders correctly', () => {
  const tree = renderer
    .create(
      <AppProvider>
        <{{name}}Grid />
      </AppProvider>
    )
    .toJSON()
  expect(tree).toMatchSnapshot()
})