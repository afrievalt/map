export const upsert{{name}} = (payload) => (dispatch, getState, getFirebase) => {
  const newRef = getFirebase()
    .database()
    .ref('{{camelCase name}}')
    .push()
  newRef.set(payload)
    .then(() => dispatch({ type: 'HOME' }))
    .catch(function (error) {
      const errorMessage = error.message
      window.alert(errorMessage)
      console.log(error)
    })
}