import {
  getFuncValidateFields,
  isRequired,
  getIsTooLong,
} from '../../../form/validationHelpers'

const {{camelCase name}}Validate = getFuncValidateFields.x({
  {{#each field}}
    {{id}}: [isRequired, getIsTooLong(100)],
  {{/each}}
})

export default {{camelCase name}}Validate