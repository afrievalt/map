import React, { memo } from 'react'
import Route from '../../controls/Route'
import FloatingButton from '../../controls/FloatingButton'
import { Screen } from '../../controls/StyledMui'


const {{pascalCase name}} = () => {
  return (
    <Screen>
      <Route type='{{constantCase name}}' exact>
        todo: stuff here !!!
        <FloatingButton routeKey='{{constantCase name}}_ADD' />
      </Route>
      <Route type='{{constantCase name}}_EDIT'>
        todo: edit stuff here !!!
      </Route>
      <Route type='{{constantCase name}}_ADD'>
        todo: add stuff here !!!
      </Route>
    </Screen>
  )
}

export default memo({{pascalCase name}})
