import { Button, Container, CssBaseline } from '@material-ui/core'
import {select{{ name }}TableData} from {{lowerCase name}}List.selectors.js
import React, {memo} from 'react'

const {{name}}List = () => {
  return (
    <Container>
//todo: add list stuff
    </Container>
  )
}

export default memo({{name}}List)
